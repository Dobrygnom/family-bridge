Fix topic preparation failing after successful source reading when the grouping response omits its redundant people list. Grouping now validates recipients against the authoritative selection-stage registry; missing coverage and changed recipients still fail validation.

After updating, an interrupted coverage analysis gets one automatic recovery from the verified saved transcript and completed extraction/selection caches. Existing topics, wording and approvals remain intact; only new suggestions are appended.

Technical diagnostics now identify the failing coverage stage, fixed reason code and attempt number without exposing source text. Process failures no longer trigger an unrelated coverage retry.

Validation: focused coverage and recovery tests, TypeScript checking, and replay of the actual failing grouping response. Replay accepted both new topics and preserved all saved topic objects unchanged.
